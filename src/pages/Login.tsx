import { FormEvent, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { toast } from "sonner";
import { useAuth } from "../contexts/AuthContext";
export default function Login() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [loading, setLoading] = useState(false); const { signIn } = useAuth(); const navigate = useNavigate(); async function handleSubmit(event: FormEvent) { event.preventDefault(); setLoading(true); try { await signIn(email, password); toast.success("Logged in"); navigate("/"); } catch (error: any) { toast.error(error.message || "Login failed"); } finally { setLoading(false); } } return <div className="container-page flex min-h-[70vh] items-center justify-center py-12"><form onSubmit={handleSubmit} className="card-soft w-full max-w-md p-6 sm:p-8"><p className="label-soft">Welcome back</p><h1 className="font-display text-4xl font-extrabold text-cocoa">Login to Cub Club</h1><div className="mt-6 grid gap-4"><div><label className="label-soft">Email</label><input type="email" required className="input-soft" value={email} onChange={(e) => setEmail(e.target.value)} /></div><div><label className="label-soft">Password</label><input type="password" required className="input-soft" value={password} onChange={(e) => setPassword(e.target.value)} /></div></div><button disabled={loading} className="btn-primary mt-6 w-full py-4">{loading ? "Logging in..." : "Login"}</button><p className="mt-5 text-center text-sm font-semibold text-cocoa/55">New here? <Link to="/signup" className="font-extrabold text-cocoa">Create account</Link></p></form></div>; }
