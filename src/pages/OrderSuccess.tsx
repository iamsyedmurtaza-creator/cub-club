import { Link, useParams } from "react-router-dom";
import { CheckCircle2 } from "lucide-react";
import { shortId } from "../utils/format";
export default function OrderSuccess() { const { orderId } = useParams(); return <div className="container-page py-12"><div className="mx-auto max-w-2xl overflow-hidden rounded-[3rem] bg-white p-8 text-center shadow-soft ring-1 ring-cocoa/5 sm:p-12"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-full bg-mint text-cocoa"><CheckCircle2 size={44} /></div><p className="label-soft mt-8">Order placed</p><h1 className="font-display text-5xl font-extrabold leading-tight text-cocoa">Your Cub Club order has been placed.</h1><p className="mt-4 text-base font-semibold leading-8 text-cocoa/60">We’ll contact you soon to confirm delivery. Payment will be collected through cash on delivery.</p>{orderId ? <p className="mt-5 rounded-2xl bg-cream px-4 py-3 text-sm font-extrabold text-cocoa">Order ID: {shortId(orderId)}</p> : null}<div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link to="/shop" className="btn-primary">Continue shopping</Link><Link to="/my-orders" className="btn-secondary">View my orders</Link></div></div></div>; }
