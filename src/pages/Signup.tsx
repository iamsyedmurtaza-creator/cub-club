import { FormEvent, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { toast } from "sonner";
import { useAuth } from "../contexts/AuthContext";
export default function Signup() { const [fullName, setFullName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [loading, setLoading] = useState(false); const { signUp } = useAuth(); const navigate = useNavigate(); async function handleSubmit(event: FormEvent) { event.preventDefault(); setLoading(true); try { await signUp(email, password, fullName); toast.success("Account created. Check email if confirmation is enabled."); navigate("/"); } catch (error: any) { toast.error(error.message || "Signup failed"); } finally { setLoading(false); } } return <div className="container-page flex min-h-[70vh] items-center justify-center py-12"><form onSubmit={handleSubmit} className="card-soft w-full max-w-md p-6 sm:p-8"><p className="label-soft">Join Cub Club</p><h1 className="font-display text-4xl font-extrabold text-cocoa">Create account</h1><div className="mt-6 grid gap-4"><div><label className="label-soft">Full name</label><input required className="input-soft" value={fullName} onChange={(e) => setFullName(e.target.value)} /></div><div><label className="label-soft">Email</label><input type="email" required className="input-soft" value={email} onChange={(e) => setEmail(e.target.value)} /></div><div><label className="label-soft">Password</label><input type="password" required minLength={6} className="input-soft" value={password} onChange={(e) => setPassword(e.target.value)} /></div></div><button disabled={loading} className="btn-primary mt-6 w-full py-4">{loading ? "Creating..." : "Create account"}</button><p className="mt-5 text-center text-sm font-semibold text-cocoa/55">Already have account? <Link to="/login" className="font-extrabold text-cocoa">Login</Link></p></form></div>; }
